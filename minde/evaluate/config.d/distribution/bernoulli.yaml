# @package _global_

distribution_key: X_dim=${distribution.dim_x}__Y_dim=${distribution.dim_y}

hydra:
  sweeper:
    params:
      ++distribution._target_: distribution_generator.distributions.get_rv
      ++distribution.min_val: 0.01
      ++distribution.dim_x: ${default_grid.dimensionality}
      ++distribution.dim_y: ${default_grid.dimensionality}